import 'package:flutter/material.dart';

class UpgradeServiceScreen extends StatelessWidget {
  const UpgradeServiceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
