import 'package:flutter/material.dart';

class PreviewItem extends StatelessWidget {
  const PreviewItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Preview Page'),
      ),
    );
  }
}
